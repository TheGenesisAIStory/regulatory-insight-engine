# Intesa Sanpaolo — documenti banca-specifici

Contenuti raccomandati:

- Pillar 3 disclosures (annuale e trimestrale se disponibile).
- Bilanci annuali e note integrative (se pubbliche).
- Policy IFRS 9, note metodologiche e modelli di perdita attesa.
- Report di governance, risk appetite e ICAAP/ILAAP quando rilevanti.

Metadata: impostare `bank: "Intesa Sanpaolo"` e `doc_type` appropriato (es. `pillar3`, `ifrs_policy`, `annual_report`).
